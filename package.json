{
  "name": "root",
  "version": "1.0.0",
  "description": "",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspace=packages",
    "test": "npm run test --workspace=packages/nodeapi"
  }
}
